const EXTRA_COLS_dataAndSkippedRows = require('./data-and-skipped-rows')
const EXTRA_COLS_sheetConfig = require('./sheet-config')

module.exports = {
  EXTRA_COLS_dataAndSkippedRows,
  EXTRA_COLS_sheetConfig,
}
