const data = require('./data')
const errors = require('./errors')
const formattedErrors = require('./formattedErrors')

module.exports = {
  MISSING_COLS_data: data,
  MISSING_COLS_errors: errors,
  MISSING_COLS_formattedErrors: formattedErrors,
}
