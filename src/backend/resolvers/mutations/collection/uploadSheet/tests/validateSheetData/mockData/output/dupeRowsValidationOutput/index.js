const data = require('./data')
const errors = require('./errors')
const formattedErrors = require('./formattedErrors')

module.exports = {
  DUPE_ROWS_data: data,
  DUPE_ROWS_errors: errors,
  DUPE_ROWS_formattedErrors: formattedErrors,
}
